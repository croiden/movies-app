(function (W) {
    'use strict';
    define([], function () {
        describe('glob', function () {
            it('allows to pass in file paths', function () {
                chai.expect(10).to.equal(10);
            });
        });
    });
})(window);